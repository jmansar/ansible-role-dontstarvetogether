{% if dst_shard_is_master is true %} 
return {
	override_enabled = true,
    worldgen_preset = "{{ dst_world_preset }}", -- “RELAXED", "ENDLESS", "SURVIVAL_TOGETHER”, "WILDERNESS", or "LIGHTS_OUT" 
    settings_preset = "{{ dst_world_preset }}",
	overrides = {
	}
}
{% else %}
return {
    override_enabled = true,
    preset = "DST_CAVE",
}
{% endif %}
